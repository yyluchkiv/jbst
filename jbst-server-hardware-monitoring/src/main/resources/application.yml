#file: noinspection SpringBootApplicationYaml

# Spring Security: actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: ["health", "info"]

# Jasypt
jasypt:
  encryptor:
    algorithm: PBEWithMD5AndDES
    iv-generator-classname: org.jasypt.iv.NoIvGenerator

# jbst
jbst:
  app:
    maven:
      group-id: @project.groupId@
      artifact-id: @project.artifactId@
      version: v@project.version@
    # omitted behaviour, no spring security, port-forwarding security
    springdoc-enabled: false
  async:
    thread-name-prefix: jbstshm-async
    threads-core-pool-percentage: 50
    threads-max-pool-percentage: 100
  events:
    thread-name-prefix: jbstshm-events
    threads-core-pool-percentage: 25
    threads-max-pool-percentage: 50

